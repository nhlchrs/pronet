# Frontend Architecture After Integration

## 🏗️ Component Architecture

```
App (Auth & Socket Providers)
│
├── AuthProvider
│   ├── Register
│   ├── Login
│   ├── Verify OTP
│   ├── Logout
│   └── JWT Token Management
│
├── SocketProvider
│   ├── Real-time Notifications
│   ├── Team Updates
│   ├── Meeting Events
│   └── Analytics Broadcasts
│
└── Router
    ├── Home Page
    │   ├── BannerHome
    │   ├── ServiceSection
    │   └── TestimonialSection
    │
    ├── Service Pages
    │   ├── Services
    │   └── Service Detail
    │
    ├── Contact Page
    │   └── ContactForm (✅ API Integrated)
    │
    ├── Footer
    │   └── NewsletterForm (✅ API Integrated)
    │
    ├── Auth Pages (To Be Created)
    │   ├── Login (useAuth hook)
    │   ├── Register (useAuth hook)
    │   └── Verify OTP (useAuth hook)
    │
    ├── Protected Routes (To Be Created)
    │   ├── Dashboard (useAuth + useSocket)
    │   ├── Profile (userAPI)
    │   ├── Meetings (meetingAPI + useSocket)
    │   ├── Payments (paymentAPI)
    │   └── Analytics (analyticsAPI + useSocket)
    │
    └── Public Pages
        ├── About
        ├── Services
        ├── Pricing
        ├── Team
        ├── Blog
        └── FAQ
```

---

## 🔄 Data Flow Architecture

```
┌─────────────────────────────────────────────────────────┐
│                   React Components                      │
│                  (Pages & Components)                   │
└──────────────────────┬──────────────────────────────────┘
                       │
        ┌──────────────┼──────────────┐
        │              │              │
        ▼              ▼              ▼
   ┌─────────┐    ┌──────────┐    ┌────────────┐
   │ useAuth │    │useSocket │    │  API Calls │
   │ (Login) │    │(Real-time)   │  (Services)│
   └────┬────┘    └────┬─────┘    └──────┬─────┘
        │              │                  │
        │              │                  │
        ▼              ▼                  ▼
   ┌────────────────────────────────────────────┐
   │    AuthContext     SocketContext          │
   │  (State & Methods) (Socket Events)        │
   └────────────────────────────────────────────┘
        │              │                  │
        │              │                  │
        └──────────────┼──────────────────┘
                       │
                       ▼
        ┌──────────────────────────────┐
        │    API Service Layer         │
        │    (src/services/api.js)     │
        │  - authAPI                   │
        │  - userAPI                   │
        │  - meetingAPI                │
        │  - paymentAPI                │
        │  - etc.                      │
        └──────────────┬───────────────┘
                       │
         ┌─────────────┼─────────────┐
         │             │             │
         ▼             ▼             ▼
    ┌─────────┐  ┌──────────┐  ┌──────────┐
    │ Backend │  │ Database │  │ Socket.io│
    │ API     │  │          │  │ Server   │
    │ (Node)  │  │(MongoDB) │  │(Real-time)
    └─────────┘  └──────────┘  └──────────┘
```

---

## 📦 Service Layer Organization

```
services/api.js
│
├── authAPI                    (Authentication)
│   ├── register()
│   ├── login()
│   ├── verifyOTP()
│   ├── resendOTP()
│   ├── getAllUsers()
│   └── getUserById()
│
├── userAPI                    (User Profiles)
│   ├── getProfile()
│   ├── updateProfile()
│   └── changePassword()
│
├── sessionAPI                 (Sessions)
│   ├── createSession()
│   ├── getSession()
│   ├── updateSession()
│   └── deleteSession()
│
├── meetingAPI                 (Meetings)
│   ├── createMeeting()
│   ├── scheduleMeeting()
│   ├── getAllMeetings()
│   ├── updateMeeting()
│   └── deleteMeeting()
│
├── announcementAPI            (Announcements)
│   ├── createAnnouncement()
│   ├── getAnnouncements()
│   ├── updateAnnouncement()
│   └── deleteAnnouncement()
│
├── fileAPI                    (Files & Uploads)
│   ├── uploadFile()
│   ├── getFiles()
│   └── deleteFile()
│
├── paymentAPI                 (Payments)
│   ├── initiatePayment()
│   ├── getPaymentHistory()
│   ├── verifyPayment()
│   └── getPaymentStatus()
│
├── teamAPI                    (Teams)
│   ├── getTeams()
│   ├── createTeam()
│   ├── updateTeam()
│   ├── addTeamMember()
│   └── removeTeamMember()
│
├── analyticsAPI               (Analytics)
│   ├── getPlatformMetrics()
│   ├── getUserAnalytics()
│   ├── getRevenueAnalytics()
│   └── getTeamAnalytics()
│
└── contactAPI                 (Contact & Newsletter)
    ├── submitContactForm()
    ├── subscribeNewsletter()
    └── unsubscribeNewsletter()
```

---

## 🎣 Hooks Organization

```
hooks/useCustomHooks.js
│
├── useAuthFlow()              (Multi-step auth)
│   ├── register
│   ├── verifyOTP
│   ├── resendOTP
│   └── login
│
├── useForm()                  (Form handling)
│   ├── formData
│   ├── handleChange
│   ├── handleSubmit
│   ├── resetForm
│   └── loading/error/success
│
├── useAsync()                 (Async operations)
│   ├── execute
│   ├── status
│   ├── data
│   └── error
│
├── usePagination()            (List pagination)
│   ├── currentPage
│   ├── totalPages
│   ├── currentItems
│   ├── nextPage
│   └── prevPage
│
├── useLocalStorage()          (Data persistence)
│   ├── storedValue
│   └── setValue
│
└── useFetch()                 (Data fetching)
    ├── data
    ├── loading
    ├── error
    └── refetch
```

---

## 🔐 Authentication Flow

```
1. REGISTRATION
   ┌─────────┐         ┌───────────────┐         ┌────────┐
   │Register │ ──────► │ Backend API   │ ──────► │ Email  │
   │Form     │         │ (register)    │         │ OTP    │
   └─────────┘         └───────────────┘         └────────┘
                              │
                              ▼
   ┌─────────┐         ┌───────────────┐
   │ useAuth │ ◄─────► │ AuthContext   │
   │         │         │ (register)    │
   └─────────┘         └───────────────┘

2. OTP VERIFICATION
   ┌─────────┐         ┌───────────────┐         ┌────────┐
   │ Verify  │ ──────► │ Backend API   │ ──────► │ Verify │
   │ OTP     │         │ (verifyOTP)   │         │ OTP    │
   └─────────┘         └───────────────┘         └────────┘
                              │
                              ▼ (returns JWT)
   ┌─────────┐         ┌───────────────┐         ┌──────────────┐
   │ useAuth │ ◄─────► │ AuthContext   │ ──────► │ localStorage │
   │         │         │ (save token)  │         │ (JWT token)  │
   └─────────┘         └───────────────┘         └──────────────┘

3. LOGIN
   ┌─────────┐         ┌───────────────┐         ┌────────┐
   │ Login   │ ──────► │ Backend API   │ ──────► │ Check  │
   │ Form    │         │ (login)       │         │ Creds  │
   └─────────┘         └───────────────┘         └────────┘
                              │
                              ▼ (returns JWT)
   ┌─────────┐         ┌───────────────┐         ┌──────────────┐
   │ useAuth │ ◄─────► │ AuthContext   │ ──────► │ localStorage │
   │         │         │ (save token)  │         │ (JWT token)  │
   └─────────┘         └───────────────┘         └──────────────┘

4. SUBSEQUENT REQUESTS
   ┌─────────┐         ┌──────────────┐         ┌───────────────┐
   │Component│ ──────► │ useAuth/APIs │ ──────► │ Backend API   │
   │(useAuth)│         │              │         │ + Token       │
   └─────────┘         └──────────────┘         │ (in header)   │
                                                 └───────────────┘
```

---

## 🔌 Socket.io Real-time Flow

```
INITIALIZATION
┌──────────────┐     ┌─────────────────┐     ┌──────────────┐
│  App Load    │────►│ SocketProvider  │────►│  Connect to  │
│              │     │ (useSocket)     │     │  Server      │
└──────────────┘     └─────────────────┘     └──────────────┘
                            │
                            ▼ (emit token in auth)
                     ┌─────────────────┐
                     │ Backend Socket  │
                     │ Server Accepts  │
                     └─────────────────┘
                            │
                            ▼
                     ┌─────────────────┐
                     │ "Socket         │
                     │ connected" ✅   │
                     └─────────────────┘

SUBSCRIPTION
┌─────────────┐     ┌──────────────┐     ┌──────────────┐
│ Component   │────►│ useSocket    │────►│ Emit         │
│(Dashboard)  │     │.subscribe()  │     │"subscribe_"  │
└─────────────┘     └──────────────┘     └──────────────┘
                            │
                            ▼
                     ┌──────────────────┐
                     │ Backend Joins    │
                     │ Socket Room      │
                     └──────────────────┘

REAL-TIME EVENT
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│ New Data     │────►│ Backend      │────►│ Emit Event   │
│(Notification)│     │ Socket.io    │     │ to Room      │
└──────────────┘     └──────────────┘     └──────────────┘
                            │
                            ▼
                     ┌──────────────────┐
                     │ Frontend Receives │
                     │ Event             │
                     └──────────────────┘
                            │
                            ▼
┌──────────────┐     ┌──────────────────┐
│ useSocket    │◄────│ Update           │
│notifications │     │Notifications     │
└──────────────┘     │Array             │
                     └──────────────────┘
                            │
                            ▼
                     ┌──────────────────┐
                     │ Component        │
                     │ Re-renders       │
                     │ (shows new data) │
                     └──────────────────┘
```

---

## 📋 Request-Response Flow

```
FRONTEND                  BACKEND               DATABASE
┌────────────┐           ┌────────┐           ┌────────┐
│ Component  │──────────►│ API    │──────────►│MongoDB │
│ (useAuth)  │ POST /api │Endpoint│ Query    │        │
│ login()    │ + Token   │(route) │           │        │
└────────────┘           └────────┘           └────────┘
                              │                    │
                              │◄───────────────────│
                              │ Result             │
                              │ (User + Token)     │
      ┌──────────────────────────────────────┐
      │                                      │
      ▼                                      │
┌────────────┐                              │
│ AuthContext│                              │
│ saves token│◄─────────────────────────────│
└────────────┘
      │
      ▼
┌────────────────┐
│ localStorage   │
│ (persist data) │
└────────────────┘
      │
      ▼
┌────────────┐
│ Component  │
│ re-renders │
└────────────┘
```

---

## 🔄 Form Submission Flow

```
CONTACT FORM EXAMPLE

1. USER INPUT
   ┌─────────────┐
   │ User fills  │
   │ form fields │
   └──────┬──────┘
          │
          ▼
   ┌─────────────────┐
   │ handleChange()  │
   │ updates state   │
   └──────┬──────────┘

2. FORM SUBMISSION
   │
   ▼
   ┌──────────────────┐
   │ handleSubmit()   │
   │ - validate data  │
   │ - set loading    │
   └──────┬───────────┘
          │
          ▼
   ┌──────────────────┐
   │ contactAPI.      │
   │ submitContactForm│
   │ (formData)       │
   └──────┬───────────┘

3. API CALL
   │
   ▼
   ┌──────────────────┐
   │ POST /api/       │
   │ contact/submit   │
   │ + Token (header) │
   │ + Body (JSON)    │
   └──────┬───────────┘
          │
          ▼
   ┌──────────────────┐
   │ Backend validates│
   │ & stores message │
   └──────┬───────────┘

4. RESPONSE
   │
   ▼
   ┌──────────────────┐
   │ Success response │
   │ (status 200)     │
   └──────┬───────────┘
          │
          ▼
   ┌──────────────────┐
   │ Component:       │
   │ - show success   │
   │ - clear form     │
   │ - remove loading │
   └──────────────────┘
```

---

## 📊 State Management Architecture

```
Global State
│
├── AuthContext
│   ├── user (object)
│   ├── token (string)
│   ├── isAuthenticated (boolean)
│   ├── loading (boolean)
│   └── error (string)
│
└── SocketContext
    ├── socket (Socket instance)
    ├── isConnected (boolean)
    └── notifications (array)

Component State
│
├── useForm
│   ├── formData (object)
│   ├── loading (boolean)
│   ├── error (string)
│   └── success (boolean)
│
└── useAsync
    ├── status (pending/success/error)
    ├── data (any)
    └── error (Error object)

Local Storage
│
├── token (JWT string)
└── user (JSON object)
```

---

## ✅ Integration Complete

All systems are now integrated and ready for development!

```
Frontend ◄────API────► Backend
         ◄──Socket.io─►
```

Ready to build! 🚀
